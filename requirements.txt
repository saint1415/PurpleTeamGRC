pyyaml>=6.0
requests>=2.28.0
python-dateutil>=2.8.0
colorama>=0.4.0
tabulate>=0.9.0
netifaces>=0.11.0
psutil>=5.9.0

# Optional dependencies for containerized/multi-user deployment
psycopg2-binary>=2.9  # PostgreSQL support (optional, for container mode)
flask>=3.0             # Web framework (optional, stdlib http.server works as fallback)
gunicorn>=21.2         # Production WSGI server (optional, for container mode)
